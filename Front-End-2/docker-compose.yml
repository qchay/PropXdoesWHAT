version: '3'

services:
        front-end:
                build: .
                volumes:
                        - .:/home/website
                ports:
                        - "80:5000"
                command:
                        python -u server/server.py